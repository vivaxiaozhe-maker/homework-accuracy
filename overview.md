# 「学生作业正确率」工作台 — 交付说明

## 交付物
- `学生作业正确率.html`：单文件工作台，全部 CSS/JS/SVG 内联，零外部依赖，双击即可在浏览器运行。

## 方案选择
用户明确要求 localStorage + 本地双击运行，故采用本地单文件方案（未走资料库在线部署）。

## 功能模块（4 个）
1. **今日概览**：「今天要处理」置顶——逾期未交标红 + 一键「标记已补交」，昨日未处理自动滚入今天；近 7 天正确率 <60% 的学生提示订正；4 张快捷统计卡。
2. **作业记录**：录入学生姓名（可新增）、作业时间（默认今天可改）、总题数/正确数（错误数与正确率自动实时计算）、错题号、批改图片上传（自动压缩至 800px、base64、最多 9 张、点击放大）；历史记录卡片列表，按日期倒序，支持删除（二次确认）。
3. **学生统计**：各学生平均正确率 SVG 柱状对比图（含 60% 关注线）；每名学生明细卡——平均正确率、最近趋势迷你折线、作业次数、未交次数及日期、登记今日未交。
4. **数据管理**：导出 JSON 备份 / 导入恢复（无条数上限，替换前确认）；清空示例数据（只删示例）；清空全部数据（二次确认）；累计 ≥30 条自动提示备份。

## 关键设计
- 示例数据 5 条记录 + 2 条未交（1 条逾期），日期均相对当天动态生成，页面琥珀色横幅明确标记「示例数据」，可一键清空。
- 奶油薄荷学术风：奶油底色 #FAF5E9 + 薄荷绿 #5FB89A，内联 SVG 图标与学习插画（书本、铅笔、对勾）。
- 响应式：桌面侧栏导航 / 手机底部 Tab；按钮 ≥44px、输入框 16px、适配 iPhone 底部安全区。
- 数据键：`wb_homework_accuracy_v1`，localStorage 持久化，刷新不丢。
- 已通过 node --check 语法校验。

## 迭代记录（2026-08-18 第 2 轮）
1. **上传入口升级为拖拽区**：支持点击选择或拖拽上传，兼容图片（压缩至 800px）和 PDF（≤4MB，存 base64，点击在新标签页打开），合计最多 9 个文件；PDF 以红色文件卡片缩略展示；旧记录的图片数据完全兼容。
2. **学生统计按同名合并**：同名同姓的学生在柱状图和明细卡中聚合为一组（标注「同名合并 ×N」），记录/未交次数/趋势线均按组汇总；原始数据不改动；概览页学生总数也改为按姓名去重。

## 迭代记录（2026-08-18 第 3 轮）
1. **同名合并改为可折叠**：统计卡同名学生仍聚合汇总，新增「查看每次详细记录（N 条）」折叠区，展开可见每次作业的日期、总/对/错、正确率和错题号。
2. **文件名识别自动填题数**：上传/拖拽文件时按文件名识别题数（支持「总20对16」「20题对16」「总20题 正确16」等写法），自动填入表单并提示核对。说明：纯离线单文件无法 OCR 识别图片/PDF 内容（需外部服务，违反零依赖原则），文件名识别为离线替代方案。

## 迭代记录（2026-08-18 第 4 轮）
1. **学生统计新增姓名搜索**：统计页顶部搜索框，支持姓名部分匹配，实时过滤对比柱状图和学生明细卡（含无结果提示）。

## 迭代记录（2026-08-18 第 5 轮）
1. **历史记录支持编辑**：每条记录新增「编辑」按钮，点击回填表单（含图片/PDF 附件），保存时更新原记录而非新增，按钮文案切换为「保存修改（更新原记录）」，并提供「取消修改」。

## 迭代记录（2026-08-18 第 6 轮）
1. **历史记录按姓名分组折叠**：同名同姓学生的记录归为一组（默认展开，可点标题折叠），组头显示头像、次数、平均正确率、最近日期；每次详细记录（含编辑/删除/附件）完整保留。
2. **历史记录新增姓名搜索**：列表上方搜索框，部分匹配、实时过滤分组。

## 迭代记录（2026-08-19 第 7 轮）
1. **录入表单姓名下拉同名合并**：同名同姓只显示一个选项（取该姓名第一个学生 id 为代表）；「录入订正」「编辑记录」回填时自动映射到代表选项。
2. **新增科目字段**：录入表单增加「科目」输入框（带历史科目下拉建议，保存后保留本科目方便连续录入）；记录卡片和统计页详细记录均显示科目标签；示例数据补充数学/语文/英语科目；不填则记为「未指定」。

## 迭代记录（2026-08-19 第 8 轮）
1. **科目改为三级联动下拉**：一级「学科/竞赛/语培」→ 二级（学科：AP/IB/AL；竞赛：AMC10/AMC12/ABO/BPHO/BBO/UKCHO/物理碗；语培：托福/雅思/SAT/ACT）→ 三级（AP 22 门、IB 6 门、AL 6 门；竞赛与语培无三级）。一级含「自定义…」兜底旧数据与菜单外科目；存储格式为「学科 / AP / 微积分BC」路径字符串；编辑回填自动逐级选中；示例数据科目同步更新为新菜单值。

## 迭代记录（2026-08-20 第 9 轮）
1. **柱状图改为按科目统计**：取消「各学生平均正确率对比」，改为「各科目平均正确率（全体学生）」——每根柱子代表一个科目下所有学生的平均正确率，柱下标注记录次数；姓名搜索时图表同步变为该学生的分科目正确率。
2. **学生明细卡改为分科目正确率**：移除卡片上的全科目总平均徽章，改为每个科目一个徽章（短名如「AP·微积分BC」+ 正确率 + 次数），颜色沿用 ≥85 绿 / 60-84 黄 / <60 红。

## 迭代记录（2026-08-20 第 10 轮）
1. **登记今日未交支持分科目**：学生明细卡点击「登记今日未交」弹出科目选择弹窗（复用三级联动菜单），未交记录携带科目路径；「今日概览」待处理项和明细卡未交列表均显示科目短名；同一学生同一天同一科目防重复登记；旧的未交记录（无科目）照常显示。

## 迭代记录（2026-08-20 第 11 轮）
1. **未交记录可编辑**：「今日概览」每条未交记录新增「编辑」按钮，弹窗可修正日期和科目（三级菜单自动回填）；登记弹窗本身也加了日期选择，可补登非当天的未交；防重复校验按 学生+日期+科目 且排除自身。

## 迭代记录（2026-08-21 第 12 轮）
1. **修复图表“乱码”并改为横向条形列表**：根因是旧 SVG 图表中 `'" y="' + 数字 - 6` 的运算符优先级 bug 产生 NaN，破坏了 SVG 标签，浏览器把图表代码当文本显示。现将「各科目平均正确率」重写为 HTML 横向条形列表：按 学科→竞赛→语培→其他 分组排序，每行 科目名（悬停显示完整路径）+ 进度条（含 60% 虚线关注线）+ 正确率 + 次数；移动端自适应。已通过 DOM 桩冒烟测试验证输出无 NaN、无残留 SVG。

## 迭代记录（2026-08-21 第 13 轮）
1. **同名学生禁止录入**：新增学生时若姓名（去空格后）已存在，拦截保存、弹窗提示，并自动在下拉中选中已有学生；配合此前下拉同名合并，杜绝重复学生条目。

## 多人使用方案（2026-08-21 用户咨询）
- 当前单文件 + localStorage 架构：数据存在「各自设备的浏览器」里。将页面部署为一个在线链接后，每位老师用自己的手机打开同一链接，各自的数据天然隔离、互不可见、互不干扰——无需账号系统即可满足「各自记录自己学生」的诉求。
- 若后续需要「同一账号跨设备同步」或「管理员查看全部数据」，则需引入后端账号体系，会破坏单文件零依赖原则，届时单独评估。

## 部署记录（2026-08-21）
- 已将当前版本（第 13 轮迭代后）部署上线，分享链接：https://955fe01028d147358d265fc99e7d4528.app.workbuddy.link
- 部署目录：`dist/`（`index.html` 为工作台副本）；后续迭代流程：改 `学生作业正确率.html` → 校验 → 拷贝覆盖 `dist/index.html` → 重新部署（链接不变，用户数据不受影响）。

## 迭代记录（2026-08-25 第 14 轮）
1. **「学生统计」改名「现有学生」**：桌面侧栏与手机底部 Tab 同步改名。
2. **新增「历史学生」板块**：与「现有学生」「作业记录」平级（侧栏/底部 Tab 均有入口，毕业帽图标）。现有学生明细卡可「转为历史学生」（二次确认，归档不删记录），归档后从录入下拉和现有统计中消失；历史学生页展示学校/毕业届/历史作业次数/平均正确率，可「恢复为现有学生」。
3. 附带完成：学生明细旁「新增学生」按钮 + 弹窗（姓名/学校/毕业年份下拉，当年起 11 年，同名拦截）；学生卡显示学校与毕业届标签。
- 已通过 DOM 桩冒烟测试：归档后现有列表、录入下拉均不含该学生，历史列表正常展示且可恢复。

## 迭代记录（2026-08-25 第 15 轮）
1. **学生信息可修改**：「现有学生」明细卡中的姓名变为可点击链接（薄荷绿虚线 underline），点击弹出「修改学生信息」窗口（复用新增弹窗），可修改姓名/学校/毕业年份；同名冲突检查（排除本组），修改自动应用到该同名组全部档案，作业记录自动跟随。

## 迭代记录（2026-08-25 第 16 轮）
1. **科目徽章变 Tab + 卡内快速录入**：「现有学生」明细卡中的科目徽章可点击，展开该科目的录入面板（日期默认今天可改、总题数、正确题目数、错误数与正确率实时计算、错题号），保存直接写入该学生该科目的作业记录；再点同一徽章收起；同一时间只展开一个面板。已通过 DOM 桩冒烟测试（展开/计算/保存/收起全链路）。

## 迭代记录（2026-08-26 第 31 轮）
1. **打卡区「今日未交」按钮**：「打卡情况」标题行右侧新增「今日未交」按钮（历史学生只读不显示），点击即为该学生×科目登记今日未交，在打卡格子中按日期顺序生成对应一次（显示日期、正确率 0%、错题 0），可点击格子查看/删除。
2. **打卡格子合并未交**：打卡格子数据源由纯作业记录改为「记录 + 未处理未交」按日期合并排序；未交格子红底「未交」标记；已处理（补交）未交不占格子；保存/删除打卡均按合并序列定位，保证「第 N 次」编号稳定。
3. **移除学生卡底部「登记今日未交」按钮**（与打卡区按钮功能重复），删除 openMissModal 函数；未交记录的编辑入口保留在「今日概览」每条记录的「编辑」按钮。
- 测试：44 项断言全 PASS；dist 已同步并重新部署（链接不变）。

## 迭代记录（2026-08-26 账号体系第 1 轮 · 前端）
1. **账号系统（前端 mock，后端后置）**：新增登录屏（账号+密码），会话存 sessionStorage（刷新保持、关浏览器重登）；密码 SHA-256(salt+密码) 经 crypto.subtle 散列，不存明文。首次运行自动播种教务 `admin`/`admin123`（首登强制改密）+ 示例助教 `ta1`/`ta2`（`ta123456`，各带示例学生数据）。
2. **权限模型**：数据池升级为 `wb_ha_v2_data`（students/records/missed 每条带 `ownerId` 归属助教；旧 key `wb_homework_accuracy_v1` 不迁移不删除）。助教仅见/仅可改自己名下数据；教务默认可查看并编辑全部，topbar「数据范围」下拉可收窄到指定助教。所有写操作自动打 ownerId（教务在指定助教视角录入归属该助教），编辑/删除/录入前有越权校验。
3. **教务「账号管理」页签**（仅教务可见，侧栏+底部 Tab）：助教列表（姓名/账号/学生数/状态）、创建助教（账号唯一）、重置密码（生成新初始密码+下次登录强制改密）、停用/启用（停用拒登但数据保留；不能停自己、不能停最后一个教务）。
4. **学生归属转移**：教务视角学生卡显示「归属：xxx」标签，点击弹出转移窗口（二次确认，学生+作业记录+未交记录一并改归属）。
5. **Api 抽象层**：数据/账号操作收敛到 `Api` 对象（当前 LocalApi=localStorage），方法签名与约定的 REST 接口（`POST /api/login`、`GET /api/state?owner=`、`GET/POST /api/users`、`POST /api/students/:id/owner` 等）一一对应，后端（Node+Express+SQLite）就绪后替换为 HttpApi，UI 零改动。
6. **助教视角适配**：导出/导入/清空只作用于自己数据（导入强制覆盖 ownerId 为自己）；示例横幅按范围生效；侧栏脚注更新为「账号体系 · 演示环境：数据暂存本机」。
- 测试：`node --check` 语法通过；DOM 桩冒烟测试 33 项断言全 PASS（助教隔离、教务全量/范围切换、停用拒登+数据保留、强制改密、账号唯一性、归属转移跟随、导出仅自己、越权防护）。dist 已同步。
- 遗留：重置密码经 alert 展示（演示期可接受）；教务在「全部数据」视角新增学生归属教务自己（UI 有提示，需要时可加归属下拉）。

## 迭代记录（2026-08-26 账号体系第 2 轮 · 前端）
1. **修复教务账号无法登录**：根因是密码散列依赖 `crypto.subtle`，该 API 在 `file://` 等非安全上下文（如 Safari 双击打开本地文件）中不可用，导致账号播种失败、任何账号都登不上。已替换为纯 JS 同步 SHA-256 实现（与标准 SHA-256 结果一致，已存散列保持有效；冒烟测试与 Node 标准实现比对含中文/长输入 4 组全 PASS）。
2. **登录界面新增角色选择**：账号密码框上方增加「助教 / 教务」分段选择（默认助教）；所选角色与账号实际角色不符时拒绝登录并提示应切换的角色。
3. **演示账号取消首登强制改密**：admin/ta1/ta2 播种时 `mustChangePwd=false`，登录直接进入主界面；已在旧版本播种过的浏览器会自动清掉该标记。「强制改密」机制保留，仅作用于教务新建/重置密码的账号。
- 测试：`node --check` 通过；DOM 桩冒烟测试 38 项断言全 PASS（新增 SHA-256 正确性、角色不符拒绝、演示账号直接登录）。dist 已同步。

## 迭代记录（2026-08-26 账号体系第 3 轮 · 教务数据看板）
1. **新增「数据看板」页签（仅教务）**：侧栏顺序为 今日概览 → 数据看板 → 现有学生 → 历史学生 → 账号管理 → 数据管理；移动端教务端底部 Tab 移除低频的「数据管理」（仅侧栏），看板入底部 Tab（保持 5 个）。助教端无看板入口，直接访问也被守卫拦截。
2. **筛选条**：时间范围（近 7 / 30 / 90 天 / 全部，默认近 30 天）+ 助教下拉（含下钻联动）；基于 pool 全量上帝视角，与 topbar「数据范围」独立。时间范围只作用于录入/正确率类指标，逾期未交与沉睡学生为当前状态指标（页底注明口径）。
3. **六大板块**：全局概览 6 卡（现有/历史学生、范围录入、范围平均正确率、未交含逾期红字、有录入助教比、今日录入）；近 30 天录入趋势（纯 div 柱，柱色按当日平均正确率，吸取第 12 轮 SVG NaN 教训）；助教维度对比（录入次数条形 + 正确率徽章 + 未交/逾期 + 最近录入时间，≥7 天未录入显示「沉默」徽章，点击行下钻该助教）；学科维度（一级类目行 + 具体科目 subj-row 条形，<60% 科目标红）；预警名单三卡（低分学生 <60%、逾期未交按天数降序、沉睡学生 ≥14 天含「从未有记录」），低分/沉睡可点击跳「现有学生」并自动填搜索框，逾期跳「今日概览」。
- 测试：`node --check` 通过；DOM 桩冒烟测试 60 项断言全 PASS（新增 22 项：板块输出无 NaN、助教筛选、时间口径、沉默徽章、三类预警构造数据逐一验证、下钻联动、助教端守卫）。dist 已同步。
- 遗留：趋势柱仅在每月 1 号与今天下方显示日期标签；沉睡学生按单个条目判定（未做同名组合并）。
- 修复（2026-08-26）：科目条形图行距不一致——根因是表头/行混用 margin 且发生折叠（表头还可能折行），改为 `.chart-wrap` flex 列布局 + 统一 `gap:10px`，表头 `white-space:nowrap` 禁折行；统计页与看板学科条形共用同一套规则。
- 修复（2026-08-26）：示例横幅/备份提示 div 缺 class 导致图标 SVG 撑满整页，补上 `sample-banner`/`backup-tip` 类名并默认 display:none。

## 迭代记录（2026-08-26 账号体系第 4 轮 · 计划次数修改审批流）
1. **「应完成作业次数」二次修改需教务审批**：助教首次设置仍直接保存；已有次数再修改时弹出申请窗口（显示 学生/科目/旧→新次数，可选填理由，文案说明需教务审核），确认后生成 pending 申请且当前次数不变；新值=旧值、已有 pending 重复申请均被拦截。
2. **助教端状态可见**：打卡面板次数行旁显示琥珀「修改审核中（N → M）」+「撤回申请」（撤回后可重新发起）；最近一次被驳回显示红色提示。
3. **教务审批模块（并入「账号管理」页顶部）**：待审批列表（申请助教/学生/科目/旧→新/理由/时间，通过直接生效、驳回需确认）+ 最近 10 条已处理折叠区；侧栏与底部 Tab 的「账号管理」按钮有待审批红点徽章（仅教务，0 隐藏）。教务在打卡面板直改次数免审批，若该科目有 pending 申请则确认后自动驳回。
4. **数据与接口**：`pool.planRequests`（pending/approved/rejected/cancelled 四态）；Api 层新增 `createPlanRequest`/`cancelPlanRequest`/`reviewPlanRequest`（助教越权校验），REST 约定已写入文件头注释。
- 测试：`node --check` 通过；DOM 桩冒烟测试 80 项断言全 PASS（新增 20 项覆盖申请/撤回/审批/直改/越权/徽章全链路）。
- 遗留：申请时间戳粒度为日期；清空全部数据不影响 planRequests 留档（学生被删后审批区显示「（已删除学生）」）。

## 迭代记录（2026-08-26 账号体系第 5 轮 · 审批流示例数据）
1. **审批模块示例数据**：每位示例助教播种 待审批/已通过/已驳回 各 1 条计划修改申请（周子墨 AMC10 8→10 待审批、苏晚晴 托福 12→15 已通过、陈星宇 AP·物理1 8→20 已驳回，均带理由与 sample 标记），并为对应示例学生补应完成次数，打卡格子同步生效。
2. **兼容已有浏览器**：`_ensureSeed` 增加一次性注入（pool.planReqSampled 标记），旧版本已播种的浏览器打开新版自动补齐审批示例；「清空示例数据」后不会再次注入，且清空/重新载入示例均覆盖 planRequests。
- 测试：`node --check` 通过；DOM 桩冒烟测试 84 项断言全 PASS（新增 4 项：示例申请播种数量/状态/标记、重复初始化不重复注入）。dist 与仓库根 index.html 已同步。

## 迭代记录（2026-08-26 账号体系第 6 轮 · 助教维度筛选 + 历史学生示例）
1. **学生明细按助教维度筛选（仅教务）**：「现有学生」明细卡上方新增助教筛选 chips 行（「全部学生」+ 各助教，停用标注，移动端横向滚动）；默认「全部学生」时明细**按助教分组**展示（组头：助教姓名 + 学生数），选中某助教只显示其学生；与姓名搜索叠加生效；上方科目图表不受此筛选影响。助教端不显示该行。
2. **历史学生示例数据**：每位示例助教播种 2 名历史学生（李浩然·深圳外国语学校·2025 届、赵雨桐·杭州外国语学校·2024 届，各 2-3 条较早作业记录 + 1 条已处理未交，避开近 30 天趋势图）；`pool.alumniSampled` 一次性注入标记，旧浏览器打开自动补齐，清空示例后不重复注入；「重新载入示例数据」同步包含。
- 测试：`node --check` 通过；DOM 桩冒烟测试 96 项断言全 PASS（8 项旧断言按新数据量更新，新增 12 项覆盖筛选 chips/分组/搜索叠加/历史学生播种与清除）。dist 与仓库根 index.html 已同步。
- 修复（2026-08-26）：助教筛选 chips 选中态 outline 被横向滚动容器裁切（padding 补足）；「重新载入示例数据」在教务「全部数据」视角下改为按助教分发（每位助教一套，含审批申请与历史学生），不再全部归属教务账号。冒烟测试增至 99 项全 PASS。

## 迭代记录（2026-08-26 账号体系第 7 轮 · 移动端适配修复）
1. **体检方法修正**：headless Chrome 窗口最小 500px 导致此前移动端截图为假象（500px 渲染裁成 390px），改用 CDP `Emulation.setDeviceMetricsOverride` 获得真实 390px 视口逐页体检。
2. **学生明细卡头部挤压**（现有/历史共用）：移动端头像+姓名+徽章独占一行自然换行；趋势迷你折线固定 120×40 与「新增科目」按钮落第二行，不再挤压徽章；统计行收紧。
3. **topbar 移动端从 6 行压到 4 个紧凑行**：隐藏学习插画、标题与日期同行、用户区小号按钮、收紧间距。
4. **看板趋势图「今天」标签贴边溢出**：最后一根柱的日期标签左移收入卡片内；账号行按钮防压缩。
- 验证：6 个页面真 390px 截图复检无溢出/无裁切；桌面端 1280px 修复前后逐字节一致（零回归）；`node --check` 通过；冒烟测试 99 项全 PASS（纯 CSS 变更）。dist 与仓库根 index.html 已同步。

## 迭代记录（2026-08-27 账号体系第 8 轮 · 《教务系统修改意见 V1》全量落地）
1. **改名「学情跟踪平台」**：title、侧栏品牌、登录页、topbar、报告页脚落款全部更新。
2. **助教端**：①「今天要处理」新增计划停滞提醒——以应完成次数设定日为起始，某科目超 7 天无新记录即提醒（新增 `subjPlanSetAt` 字段，首次设置与审批通过时写入，旧数据不回溯防刷屏）；②应完成次数首次「保存」后按钮变「修改」，修改走教务审批，提交后显示「已发送教管审批（N → M）」+ 撤回；③生成报告新增第四节「学习计划与建议」（老师手填，存 `subjAdvice`，AI 生成按文档意见待定）；④助教端数据管理移除清理数据三按钮（清空示例/重新载入/清空全部，含示例横幅按钮，仅教务可见可操作）。
3. **教务端**：①看板板块顺序调整为 全局概览→预警名单→录入趋势→助教维度→学科维度，沉睡学生口径 ≥14 天改 ≥7 天，三个名单默认前 5 条可展开；②「计划修改审批」从账号管理迁入今日概览「今天要处理」顶部，红点徽章同步迁移，教务视角未交/提醒卡片全部只读（无补录/删除按钮）；③「全部学生」视图按助教分组默认折叠（点击组头展开，搜索时自动展开匹配组）；④历史学生页加姓名/学校搜索栏；⑤topbar 移除「数据范围」下拉（教务恒全部视角），打卡面板次数行对教务只读（计划变更只走审批）。
4. **新增销售（sales）角色**：登录页角色分段加「销售」，教务可创建销售账号，播种演示账号 `sales1/sales123456`（张顾问，老浏览器自动回填）；销售端仅「现有学生/历史学生」两个页签，默认无列表、输入关键字才显示只读学生卡（含归属助教、科目进度，零操作按钮、不可写任何数据、无导出），防批量泄露。
- 测试：`node --check` 通过；DOM 桩冒烟测试 151 项断言全 PASS（批次一 118 → 批次二 151，旧断言 6 处随功能调整更新）；CDP 真 390px 实测教务今日页/看板/账号页/销售页均正常。dist 与仓库根 index.html 已同步。

## 迭代记录（2026-08-27 账号体系第 9 轮 · 已处理事项回溯）
1. **「今天要处理」底部新增「已处理事项（近 30 天）」折叠区**：合并两类条目按处理时间倒序——已处理未交（已补交=绿/已删除=灰徽章，学生+科目+原日期+处理日期，教务附归属助教）与已处理审批（已通过=绿/已驳回=红，旧→新次数+申请助教）；默认前 10 条可展开全部，无数据不渲染，销售端不渲染。
2. **未交记录软删除留痕**：missed 增加 `resolvedAt`/`resolution`（made-up/deleted）字段；「标记已补交」与「删除」均改为标记不再物理删除，删除确认文案注明可在已处理事项回溯；未处理列表/打卡格子/逾期统计口径（`!resolved`）不受影响。
3. 审批区原「最近已处理（最多 10 条）」折叠区移除，并入统一回溯区（助教：自己的未交处理 + 自己申请的结果；教务：全部）。
- 测试：`node --check` 通过；DOM 桩冒烟测试 165 项断言全 PASS（新增 13 项：留痕字段、软删除、角色口径、30 天窗口、折叠展开）；CDP 真 390px 实测正常。dist 与仓库根 index.html 已同步。

## 迭代记录（2026-08-27 账号体系第 10 轮 · 操作记录审计回溯）
1. **审计日志数据层**：`pool.auditLogs`（ts 精确到秒、操作人/角色、中文动作名、targetType 七类、对象描述、详情）；`logAction()` 统一打点，接入全部 24 处写操作（作业/未交/学生/计划审批/评语模考建议/账号/登录改密/导入清空）；超 5000 条 FIFO 裁剪（mock 期容量保护，后端进 SQLite 无上限）。
2. **「操作记录」页签**：搜索框（学生名/操作人/详情关键字实时过滤）+ 类型下拉 + 时间范围（今天/7/30 天/全部）+ 结果统计 + 倒序日志列表（时间、操作人角色徽章、动作徽章分色、对象、详情）+ 默认 50 条加载更多。
3. **角色口径**：教务全量；助教只见自己名下学生相关 + 自己的操作；销售无入口且守卫拦截。底部 Tab：助教加「记录」为第 5 个，教务「账号」移出底部（侧栏保留）腾位给「记录」。
- 测试：`node --check` 通过；DOM 桩冒烟测试 191 项断言全 PASS（新增 26 项：打点抽查、搜索/筛选/分页、助教隔离、裁剪、销售拦截）；CDP 真 390px 实测正常。dist 与仓库根 index.html 已同步。
- 遗留：审计日志随导出备份包含但导入不回放（保持审计追加性）；裁剪掉的历史不可恢复（mock 期限制）。

## 迭代记录（2026-08-27 账号体系第 11 轮 · 操作记录演示数据）
1. **操作记录预填演示数据**：`seedAuditSamples()` 为每位示例助教播种 5+ 条日志（录入作业/登记未交/提交计划修改申请/保存评语/新增学生/登录）+ 教务 4 条（创建账号/审批通过/审批驳回/登录），均带 sample 标记、ts 精确到秒、时间分布在近一周内。
2. 沿用一次性注入模式（`pool.auditSampled` 标记，老浏览器打开自动补齐，清空示例后不重复注入）；「清空示例数据」按归属同步清除示例日志；「重新载入示例数据」同步补播种（教务=全量，助教=自己）。
- 测试：`node --check` 通过；DOM 桩冒烟测试 196 项断言全 PASS（新增 5 项：播种数量/ts 格式/类型覆盖/教务审批记录/防重复注入）。dist 与仓库根 index.html 已同步。

## 迭代记录（2026-08-28 账号体系第 12 轮 · 销售账号展示 + 温暖短句）
1. **登录页演示账号区补充销售账号**：sales1 / sales123456（张顾问）。
2. **顶部温暖短句**：topbar 账号名下方新增一行右对齐淡色小字，预置 16 句温暖舒缓型短句（不鸡血）；计数存 localStorage，每 10 次页面刷新轮换一句（`renderMotto()`，`wb_ha_v2_motto` 计数器）。
- 测试：`node --check` 通过；冒烟测试 200 项断言全 PASS（新增 4 项：销售账号展示、短句渲染、句子库来源、10 次轮换）。桌面 1280px 截图核对布局整齐。dist 与仓库根 index.html 已同步。

## 迭代记录（2026-08-28 账号体系第 13 轮 · 销售面板示例数据）
1. **销售端无关键字时展示示例学生**：现有学生/历史学生页默认显示带「示例」标记的只读示例学生卡（顶部提示"以下为示例学生（演示用）；查询真实学生请输入姓名或学校"）；输入关键字后照常全局搜索。保留防批量泄露设计——真实学生仍需搜索才可见。
- 测试：`node --check` 通过；冒烟测试 201 项断言全 PASS（旧断言"无关键字不渲染学生卡"更新为"展示示例卡且只读"）；CDP 真 390px 实测正常。dist 与仓库根 index.html 已同步。

## 迭代记录（2026-08-28 账号体系第 14 轮 · 销售科目只读详情）
1. **销售端科目徽章可点击展开只读详情**：现有/历史学生卡的科目徽章点击后展开「作业打卡（只读）」面板——应完成次数、打卡格子（复用格子样式，去掉全部 onclick/补录提示）、老师评语、学习计划与建议、模考信息，全部只读；再点同一徽章收起。
- 测试：`node --check` 通过；冒烟测试 204 项断言全 PASS（新增 3 项：展开含四大区块、面板只读无输入框/编辑入口、收起）；CDP 真 390px 实测正常。dist 与仓库根 index.html 已同步。

## 后续可迭代
- 后端搭建（Node + Express + SQLite，按前端 Api 层注释中的 REST 约定实现 HttpApi 替换）、HTTPS/token、按科目筛选历史记录、单个学生长期趋势大图、CSV 导出。
